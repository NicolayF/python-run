# python-run
This project analyzes FlowApp user reviews using Python and pandas. The script cleans the dataset, removes invalid values and duplicates, calculates the most frequent words for each rating, and generates basic statistics.
